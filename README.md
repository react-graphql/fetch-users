## GraphQL + React + SQLite
This POC we used ReactJs and GraphQL to fetch data using SQLite DB.

### Install packages
```bash
#Install SQLite
cd server
npm install sqlite sqlite3
```

### sun application
```bash
cd server
npm run dev

cd client
npm run dev
```

###
